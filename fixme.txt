Fixme:


* RAND_add can take a double as parameter in the OS4 version of the library
* solve the incompatibilities in the PPC stubs for the m68k version
* Make sure that the library uses libcmt's putc macro and not clib2
* Expunge code for OS4 lib
