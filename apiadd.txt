
bn
--

functions:
void	BN_CTX_start(BN_CTX *ctx);
BIGNUM *BN_CTX_get(BN_CTX *ctx);
void	BN_CTX_end(BN_CTX *ctx);
int     BN_pseudo_rand(BIGNUM *rnd, int bits, int top,int bottom);
int	BN_rand_range(BIGNUM *rnd, BIGNUM *range);
int	BN_pseudo_rand_range(BIGNUM *rnd, BIGNUM *range);
int	BN_mod_exp_mont_word(BIGNUM *r, BN_ULONG a, const BIGNUM *p,
			const BIGNUM *m, BN_CTX *ctx, BN_MONT_CTX *m_ctx);
int	BN_is_prime_fasttest(const BIGNUM *p,int nchecks,
		void (*callback)(int,int,void *),BN_CTX *ctx,void *cb_arg,
		int do_trial_division);

actions taken:
Added to amissl_lib.fd amissl_lib.fd_amissl and amissl_pragmas.h


rand
----

functions:
int  RAND_pseudo_bytes(unsigned char *buf,int num);
void RAND_add(const void *buf,int num,double entropy);
int RAND_status(void);
int RAND_egd(const char *path);
int RAND_egd_bytes(const char *path,int bytes);
int RAND_poll(void);
void ERR_load_RAND_strings(void);

actions taken:

Added to amissl_lib.fd amissl_lib.fd_amissl and amissl_pragmas.h

FIXME! Fix the API so that the double is passed as float in dx instead

dsa
---

functions:
DSA_is_prime is removed


ssl
---

renamed SSLeay_add_all* to OpenSSL_add_all*

cryptlib
--------

CRYPTO_dbg_remalloc is obsolete


bio
---

BIO_printfA changed into: int BIO_printfA(BIO *bio, char *format, void **args)
